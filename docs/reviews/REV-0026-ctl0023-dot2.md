# REV-0026 — Soi CTL-0023 Đợt 2 (cam ra mặt tiền + thanh bên sáng)

**HỒ LY** · 2026-08-28 · `d1673dc → 328331a` · nhánh `feature/ctl-0023-dot2-cam`
**KẾT LUẬN: FIX_REQUIRED** — **KHÔNG chỗ nào tàng hình**, CSS **đạt và đẹp thật**; còn **4 dòng màu phải sửa kèm** (3 chỗ điện thoại giữ màu tối cũ + 1 chỗ trong `app.js`). Sửa xong đẩy ngay. **Lần này TÔI CHỤP ĐƯỢC ẢNH** — Chrome headless có sẵn trên máy, chi phí 0: `docs/reviews/anh-rev0026/`.

## 1. ⚠️ CHỮ TRẮNG TRÊN NỀN TRẮNG — **SÓT 0 CHỖ.**

Bàn đo **tôi tự dựng**, không đụng script của nó: dựng cây DOM thật từ `app.html`, **tiêm thêm phần nav do `app.js` d.1527 sinh** (`.sb-item` thường/`.active`/`.locked` + `svg.ico` + span) và 5 biến thể `.thd-*` mà `app.js` d.1423 gán, rồi **leo tổ tiên tìm nền hiệu lực**, pha alpha, đo cả 3 trạng thái. **108 cặp trong thanh bên. Thấp nhất 4,23. Không cặp nào dưới 3.**

| Soi cái gì | Kết quả |
|---|---|
| Biểu tượng menu | `stroke="currentColor"` (`app.js` d.1529) → theo `.sb-item`, không dán cứng màu ✔ |
| Đường kẻ nhóm | `.sb-brand`/`.sb-foot`: `rgba(255,255,255,.11)` → `var(--line)` ✔ |
| Chấm "Đang làm việc" | `currentColor`, nhận `--ok-dark`/`--sage-dark`/`--warn-dark`/`--danger`/`--text-mute` ✔ |
| Ảnh đại diện · tấm lót logo | `--ink` trên `--sage-logo` = **5,53** ✔ · bỏ nền, **giữ nguyên padding** ✔ |
| `rgba(255,255,255,…)` | **0 chỗ còn trong thanh bên** (7 chỗ còn lại đều ngoài: login-hero ×3, vd-panel ×2, mt-panel ×2) |
| `:hover`/`:active`/`:focus` | đủ, kể cả `.sb-item.active:hover` và `.sb-item.locked:hover` ✔ |
| Thanh cuộn | `::-webkit-scrollbar` **chỉ** đặt cho `.table-wrap*`; thanh bên dùng mặc định OS → tự theo nền sáng ✔ |
| **Khối dưới cùng** (ảnh Sếp gửi) | tên 15,13 · chức danh 5,30 · nhãn t.thái 4,58 · Đăng xuất 5,30 ✔ |

Nhưng bắt được **4 cặp TỆ ĐI, 1 rớt qua ngưỡng** — cùng một gốc, mục 3.

## 2. `<meta theme-color>` — đúng ở `app.html`, **SÓT 3/4 chỗ. Đây là lý do FIX_REQUIRED.**

`app.html` d.10 `#3f4d33 → #fbf9f5` **có thật, đúng**. Đi tìm chỗ khác thì: **`index.html` d.6** vẫn `theme-color:#3f4d33` — **màn ĐĂNG NHẬP, màn đầu tiên Sếp mở trên điện thoại**; **`manifest.webmanifest` d.11** `theme_color:#3f4d33` (thanh trên khi cài PWA, `start_url:/app.html`) và **d.10** `background_color:#f2f1ee` (màn khởi động, `--surface` CŨ); **`reset.html` d.9** còn nguyên `--sage:#9aab86 --ink:#3f4d33 --bg:#f2f1ee` (trang dọn kẹt, THẤP). Nó tự viết lý do đổi là *"trên điện thoại nó vẽ một dải ô liu tối ngay trên đầu ERP"* — **đúng lý do đó áp cho cả 3 chỗ còn lại**, mà chỉ vá 1. Biểu tượng PWA/apple-touch chỉ là logo, **không cần đụng**.

## 3. Số đo — **31/34 con số tự tính lại KHỚP TỪNG CHỮ SỐ. 1 chỗ sai thật.**

Khớp: L\* `--sidebar-bg` **97,97** · L\* `--ink` **13,23** · ΔL\* thanh bên↔`.main` **4,31** · `--ink`/sidebar-bg **15,13** · `--text` **11,42** · `--text-mute` **5,30** · `--ink`/`--cam` **5,63** · hover **13,54** · chọn+hover **6,20** · trắng/`--cam-dark` **4,91** · hover **6,15** · trắng/`--cam` **2,83** · `--cam-text` (card/surface-2/bg/cam-wash) **5,61 / 4,61 / 4,78 / 5,21** · `--cam-dark` trên surface-2 **4,03** · `--ok-dark` **5,19 / 5,99 / 4,92** · `--ok` cũ **3,15 / 3,64** · nút trang mẫu **2,61** · nháp hover sai **3,24**. Hai số `.mt-panel` **5,17 / 5,02** cũng đúng — phải **pha `rgba(255,255,255,.92)` lên gradient** mới ra (đo thẳng lên gradient là 1,87).

**Sai 1 chỗ, và nó lộ ra bàn đo VẪN CÒN DỐI.** Chú thích d.874 ghi *"`--sage` ở đây 4,45:1"* cho `.thd-sage`. **Thật là 3,84** — nền của nó **không phải** `--sidebar-bg` mà là `.thd-nut{background:var(--surface-2)}`. Nó khai đã sửa bàn đo, nhưng chỉ **đổi một hằng số dán cứng lấy hằng số khác** (`T('ink')` → `--sidebar-bg`) cho **cả họ** `/^\.sb-|^\.thd-(nut|cham|ok|sage|warn|danger|mute)/`. **Vẫn không leo tổ tiên** → mù nền riêng của `.thd-nut` và `.thd-nut:hover` (`--line`). → **Khai "RỚT QUA NGƯỠNG: 0" là SAI**, và *"tệ đi 9, đều còn ≥4,90"* cũng sai: `.thd-danger` **4,67→4,23** (rớt qua 4,5) · `.thd-ok` di chuột **4,87→4,40** · `.thd-mute` di chuột **4,27→4,10** · `.thd-danger` di chuột 3,68→3,79 (tốt hơn). **KHÔNG chặn phát hành**: thứ duy nhất mang màu đó là **chấm tròn 7×7px** (`#thdNhan` bị `.sb-user .meta span` đè, đặc hiệu 201 > 100) → **thành phần đồ hoạ, ngưỡng 3,0**, thấp nhất **3,79** vẫn đạt. Nhưng bàn đo phải sửa cho đúng.

**15 cặp còn trượt — có ai đọc hằng ngày không?** Trên bộ **4.443 cặp DOM thật**: **tệ đi 115 · tốt lên 233 · rớt ngưỡng 6 (đều là 3 dòng trên)**. Còn trượt đa số là **màu mang nghĩa, trượt từ trước CTL-0023, vòng này không tệ đi** (`.form-loi` 4,33 ×30 · `.mt-chip.warn` 2,82 · `.hd-han.sap-het` 3,23). **Hai chỗ đáng nêu tên, cả hai KHÔNG do Đợt 2 gây ra:**

- **`app.js` d.4711 `oLoi.style.color='var(--ok)'`** trên `.form-loi` nền `--danger-wash` = **3,05:1** — câu *"Đã đồng bộ xong: N đơn hàng (Shopee + TikTok)"*, **vận hành sàn đọc mỗi lần đồng bộ**. Nó quét sạch `var(--ok)` trong CSS nhưng **không quét JS**. Đổi 1 chữ sang `--ok-dark` → **5,02**.
- **`.vd-panel .panel-head #vd-nut-mo`** — nút "+ Khen ai đó": `#fff` trên `rgba(255,255,255,.22)` phủ dải vàng = **1,86–3,07:1**. **Anh em sinh đôi** của `.mt-panel … #cv-nut-mo-form` mà nó vừa vá (2,35–3,71 → 5,02–5,18) — **vá một, bỏ một**. Phiếu sau.

**`--ok` KHÔNG phải biến chết.** `grep var(--ok)` trong CSS ra **0** — đúng; nhưng `app.js` d.4711 **đang dùng** (chính là lỗi 3,05:1 trên). Giữ `--ok` **đúng** (chân bộ ba mang nghĩa, giá trị TÔ cho nền/chấm "đã xong", vai mà `--ok-dark` không thay được), nhưng luận cứ *"không selector nào dùng"* bỏ sót đúng chỗ đang có lỗi thật.

## 4. Mật độ dữ liệu — **0% thay đổi. Hai lớp chứng minh, không dùng bàn đo của nó.**

**Tĩnh:** tự tách **2.076 khai báo**, lọc **1.147 khai báo chiếm chỗ**. Đổi/thêm/xoá: **đúng 2**, cả hai chỉ đổi **màu** trong shorthand `border`, giữ `1px` (`.sb-brand`, `.sb-foot`). `--r-sm` 8→10px và bóng mới không chiếm chiều cao — đúng như nó nói. **Bằng pixel (mạnh hơn):** chụp cùng trang hai bản rồi so **vị trí đường kẻ ngang**. **1440×900 vùng nội dung: 54 đường ở cả hai bản, lệch lớn nhất 0px. 375×812 cả màn: 77 đường, lệch 0px.** Không dòng nào bị đẩy đi đâu.

## 5. Nó phản biện Gạo 3 lần — **ĐÚNG CẢ BA. Nói thẳng: Gạo sai.**

Tải thẳng `baikosi.netlify.app` (CSS nằm trong `<style>`, 72KB) rồi đếm:

- **"Trang mẫu KHÔNG thoáng" — ĐÚNG.** Khe hở: **trung vị mẫu 8px · ERP 10px** → **mẫu DÀY HƠN ERP**. Cỡ chữ: **trung vị mẫu 13px · ERP 12,5px** — chỗ này mẫu **to hơn** nửa pixel, nên "font 12–13px" hơi kéo về phía có lợi cho nó; kết luận *"khoảng trắng không phải thứ làm nó hiện đại"* thì **đúng**.
- **"Nút chính trang mẫu TRƯỢT" — ĐÚNG, và nhẹ hơn thực tế.** `.btn-solid{background:var(--orange);color:#fff}` có thật nguyên văn — **và còn 5 nút nữa cùng lỗi** (`.brand-cta-btn`, `.header-cta`, `.filter-btn.active`, `.btn-order`, `.card .btn-detail`). Trắng trên `#FF7A18` = **2,61:1 ×6**. Chép mù là chép 6 lỗi.
- **"Bo góc ERP vốn đã đúng thang" — ĐÚNG, đến mức đổi `--r-sm` 8→10 là THỪA.** 10/14/22/pill đều có trong thang mẫu; nhưng **8px là bo góc mẫu dùng NHIỀU NHẤT (12 lần > 10px 8 lần)** → lý do *"khớp bậc gốc trang mẫu"* **sai**, việc làm vô hại. Hai chuỗi bóng nó trích như nguyên văn: `0 4px 14px rgba(20,18,16,0.06)` **có thật**; `0 8px 30px …` và `0 6px 18px rgba(255,122,24,.34)` **không có** trong file. Nguyên tắc đúng, đừng viết như trích dẫn.

## 6. Tem in + 3 chỗ suýt hỏng — **sạch.**

**Tự băm lại:** khối `.ts-tem*` (24 dòng) `sha256 ae6a3a6d0deca67b…` **giống hệt hai bản**; `@media print` `1d12908333c1d724…` **giống hệt**; hunk diff cuối ở +2640, tem ở +2430–2453 → **0 byte**; `visibility` chứ không `display` → **ADR-0008 nguyên vẹn**. **Luật CSS 605 → 606 (+1) ĐÚNG CHÍNH XÁC** (`{`=`}`=606, khối `@`=16, luật lá 589→590, 0 selector mất). ①hover-tối-đi: đã đổi sang **sáng lên** `#f18627` = 6,20 ✔ (bản nháp đúng là 3,24). ②bàn đo: **sửa nửa vời**, mục 3. ③10 con số chú thích: đo 34, khớp 31, **sai 1**, 2 là trích dẫn không chính xác.

## 7. Bằng MẮT — **hết "đen thùi lùi" thật, không chỉ trên bảng số.**

Ảnh ở `docs/reviews/anh-rev0026/`. Một người mở ERP lên sẽ thấy: **khối gần đen rộng 244px chạy suốt từ đỉnh xuống đáy màn hình BIẾN MẤT**. Cả màn là **một dải kem liền**, thanh bên tách khỏi vùng nội dung bằng **một sợi kẻ mảnh** chứ không bằng một bức tường. Logo thôi ngồi trên tấm lót trắng vá víu. Chín mục menu từ chữ xám nhạt thành **chữ đen rõ**, mục đang mở là **một viên cam đặc** — mảng màu duy nhất trên cả thanh bên, mắt bắt được ngay. Khối dưới: tên Sếp đen đậm, chức danh xám, chấm xanh "Đang làm việc", "Đăng xuất" đọc được. **Vùng nội dung không đổi một pixel** — hai ảnh chồng lên nhau, 54 đường kẻ trùng khít. Chỗ chưa đã: nút **"+ Khen ai đó"** trên dải vàng vẫn mờ, và **trên điện thoại màn đăng nhập vẫn còn dải ô liu tối** — đúng cái Sếp chê, chỉ khác là ở màn trước đó.

## 8. Bảng lỗi

| # | Lỗi | Mức | Chặn phát hành? |
|---|---|---|---|
| H1 | `index.html` d.6 + `manifest` d.10–11 còn `#3f4d33`/`#f2f1ee` → điện thoại vẫn thấy dải tối ở màn đăng nhập & PWA | **CAO** | **Sửa kèm — 3 dòng màu, 0 rủi ro** |
| H2 | `app.js` d.4711 `var(--ok)` = **3,05:1** ("Đã đồng bộ xong…", vận hành sàn đọc mỗi lần) → `--ok-dark` 5,02 | **CAO** | **Sửa kèm — 1 dòng** |
| H3 | Bàn đo vẫn dán cứng MỘT nền cho cả họ `.sb-*`/`.thd-*`, mù nền riêng `.thd-nut` → khai "0 rớt ngưỡng" **sai**; `.thd-danger` 4,67→**4,23** | TRUNG | Không — chấm 7px, ngưỡng 3,0, thấp nhất 3,79 |
| H4 | Chú thích d.874 ghi `.thd-sage` 4,45; thật **3,84** (hệ quả H3). Cách vá đúng ✔, con số sai | TRUNG | Không |
| H5 | `.vd-panel .panel-head #vd-nut-mo` = **1,86–3,07:1** — sinh đôi của chỗ vừa vá, bỏ sót | TRUNG | Không — có từ trước |
| H6 | `--ok` khai "không selector nào dùng" nhưng **JS đang dùng** (chính là H2); giữ `--ok` vẫn đúng | THẤP | Không |
| H7 | `reset.html` d.9 còn nguyên bảng màu trước CTL-0023 | THẤP | Không |
| H8 | `--r-sm` 8→10 lý do sai; 2 chuỗi bóng trích dẫn không có thật trong trang mẫu | THẤP | Không |

**Chốt:** CSS **đạt** — 0 chỗ tàng hình, 0 pixel mật độ, tem in 0 byte, **bằng mắt đúng là sang lên thật**. Sửa **H1 + H2 (4 dòng màu)** rồi đẩy, đừng chờ vòng sau: hai lỗi đó nằm đúng chỗ Sếp sẽ nhìn (điện thoại) và chỗ nhân viên đọc hằng ngày.
