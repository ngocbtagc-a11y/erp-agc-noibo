# Changelog — ERP Alpha Green Commerce

Chỉ ghi thay đổi đáng kể (feature mới, quyết định kiến trúc, migration,
breaking change). **Không ghi từng typo/CSS nhỏ** — xem `git log` cho chi
tiết từng commit.

Format: `Date | Feature | Domain | Decision | Migration | Breaking impact | Status`

---

## 2026-09-06

| Feature | Domain | Decision | Migration | Breaking impact | Status |
|---|---|---|---|---|---|
| Màn R&D sản phẩm — quy trình phát triển sản phẩm thực phẩm A→Z (12 giai đoạn, 57 việc checklist) thay placeholder trong tab Kinh doanh | Kinh doanh (R&D — domain mới) | Viết Feature Spec trước khi code (`docs/FEATURE-SPEC-RND-SANPHAM.md`), trả lời đủ 6 Architecture Gate. Boundary **LOCAL_DOMAIN** — tự triển khai được vì: 3 bảng mới hoàn toàn (`rnd_du_an`/`rnd_buoc`/`rnd_lich_su`), **KHÔNG sửa `src/quyen.js`**, không đổi Source of Truth, không đụng domain khác. Quyết định quan trọng nhất: **tái dùng đúng bộ quyền Sản phẩm/SKU đã có** (`duocSuaSanPham` để thao tác, `duocKhoaSanPham` cho cổng Duyệt ra mắt + quyền bỏ qua cổng) thay vì đẻ vai trò/quyền R&D mới — R&D chính là việc "quyết định sẽ bán gì", cùng Data Owner với Sản phẩm/SKU đã chốt 22/08/2026; `quan_ly_kho` tuy có `sua_san_pham` nhưng không có tab `kinhdoanh` nên không với tới. Điểm khác biệt so với "checklist trang trí": **cổng chặn nằm ở MÁY CHỦ** — còn việc bắt buộc chưa xong thì `/api/rnd/chuyen-giai-doan` trả 400 kèm danh sách việc còn thiếu, không phải chỉ ẩn nút. Ngoại lệ có kiểm soát (Rule 4): chỉ người có quyền khoá SKU mới bỏ qua được, **bắt buộc nhập lý do**, lý do đi thẳng vào sổ `rnd_lich_su` với nhãn ⚠️. Quy trình để ở CODE (`KHUON_QUY_TRINH` trong `src/rnd.js`) chứ không cho người dùng tự sửa — mỗi dự án một quy trình thì hết so sánh được ai đang tắc ở đâu; dự án đang chạy giữ nguyên bộ bước lúc bắt đầu khi khuôn được sửa (Rule 10). `rnd_du_an` **không có cột tên/SKU sản phẩm**, chỉ trỏ `san_pham_id` sang `san_pham` gốc (Rule 1). Tự sửa 1 điểm sau lần test đầu: bản đầu 50/57 việc là bắt buộc — quá nặng, đúng rủi ro "người dùng bỏ giữa chừng" đã ghi trong spec, đã nới 7 việc mang tính tình huống xuống 43/57. Thêm 1 thay đổi nhỏ ở Core UI (`api.js`): khi máy chủ trả lỗi, đính nguyên JSON lên `err.du_lieu` để nơi gọi hiển thị được chi tiết (`con_thieu`) — tương thích ngược hoàn toàn, mọi chỗ dùng `err.message` không đổi. Test trực tiếp trên D1 local + trình duyệt thật: tạo dự án → sinh RD0001 + 57 bước; chặn chuyển giai đoạn khi thiếu việc; bỏ qua không lý do bị từ chối, có lý do thì qua và ghi sổ; quay lại/tạm dừng/tiếp tục/huỷ đều bắt lý do; hoàn thành bị chặn khi chưa tới giai đoạn cuối và khi còn việc bắt buộc; gắn SKU trùng dự án khác bị chặn; đăng nhập vai trò `cskh` xem được nhưng mọi mutation trả 403; tick 1 việc thì bảng nền + thẻ thống kê cập nhật ngay không cần F5; combobox trong modal không bị cắt; mobile 375px không tràn ngang. **Đã xoá toàn bộ test data sau khi verify.** | Có — `migrations/them-rnd-sanpham.sql` (3 bảng mới + 4 index, chỉ CREATE, không ALTER/DROP). **Phải chạy `node scripts/chay-migration.mjs them-rnd-sanpham.sql --remote` ngay sau khi deploy** | Không (bảng mới + API mới; `err.du_lieu` là bổ sung tương thích ngược) | **PILOT** — chưa đi trọn 1 vòng A→Z bằng dự án thật; Sếp rà lại tên 57 việc trong `src/rnd.js` trước khi coi là OFFICIAL |
| Dashboard Marketplace (Home/Dashboard Phase 3) — Tổng quan 2 sàn + 10 SKU bán chạy/bán kém | Kinh doanh (Marketplace) + Core (Home) | Audit trước khi code (`docs/audit/AUDIT-DASHBOARD-MARKETPLACE.md`). ERP Owner chốt thước đo doanh thu (nguyên văn: "tổng đơn đặt sau khi trừ hoàn hủy, khi nào phát sinh hoàn hủy thực thì trừ, đơn đang trên đường giao vẫn ghi nhận doanh thu tạm tính") và mục đích Rule D6 (xem tổng tình hình toàn công ty, phát hiện sụt giảm bất thường theo kênh, biết kênh nào hiệu quả). Ghi định nghĩa metric vào `METRIC-DEFINITIONS.md` TRƯỚC khi code, đúng thứ tự Rule D4: `doanh_thu_tam_tinh` (thước chính thức, cũng là thước đếm mục tiêu Shopee 120 tỷ/TikTok 20 tỷ) + `gmv_dat_hang`/`tien_huy`/`tien_hoan`/`ty_le_huy_hoan`/`doanh_thu_sku`. **Bỏ metric cũ `doanh_thu_hom_nay`** — tài liệu ghi "đơn thành công" nhưng `kdTongQuanDoanhThu` không lọc `trang_thai` nên cộng cả đơn hủy, tự nó vi phạm D4; xoá luôn API cũ thay vì để 2 định nghĩa song song | Không (phần này chỉ thêm API + giao diện) | Có — bỏ API `GET /api/kinh-doanh/tong-quan-doanh-thu` và metric `doanh_thu_hom_nay`; đã rà toàn repo, không nơi nào khác gọi, thay bằng `GET /api/kinh-doanh/tong-quan-kenh` | **PILOT** — đã đối chiếu với D1 production (chỉ đọc): 300 đơn thật bóc dòng khớp tuyệt đối 300/300, tập trạng thái 2 sàn xác nhận đúng, công thức doanh thu cho ~50 tỷ/năm khớp quy mô thật. Nhờ dữ liệu thật bắt thêm 3 lỗi (bộ tách giá sai 120/150 đơn Shopee, san_pham production TRỐNG nên bảng xếp hạng sẽ rỗng, so sánh kỳ trước rơi vào khoảng chưa đồng bộ ra +335.000%) — đã sửa cả 3. CHƯA mở bằng trình duyệt lần nào |
| Fix: thẻ "Doanh thu hôm nay" trên Home CEO chưa từng hiện lên lần nào | Core (Home) | Root cause: `veTongQuanTheoVaiTro()` gọi `API.kdTongQuanDoanhThu()` nhưng `api.js` KHÔNG hề định nghĩa hàm đó → `TypeError` rơi thẳng vào `catch { }` rỗng có chú thích "chưa nạp migration — im lặng bỏ qua". Backend vẫn tốt, chưa ai gọi tới. Nghĩa là Home của Sếp chưa bao giờ hiện một con số tiền nào, không lỗi trên màn hình lẫn trong log. Lỗi ngầm thứ hai nằm cùng dòng: thiếu phép chia 100000 (`tong_tien` lưu ×100000) nên kể cả sửa đúng tên hàm thì số vẫn sai gấp 100.000 lần. Sửa cả hai + ban hành: `catch` ở chỗ này bắt buộc `console.error`, không nuốt im lặng nữa | Không | Không | OFFICIAL |
| Bảng dòng hàng `don_hang_item` (Phase 3a) | Kinh doanh (Order) | Điều kiện cần cho mọi phân tích theo SKU: `don_hang` mỗi đơn 1 dòng, đơn nhiều mặt hàng bị nối chuỗi SKU thành `"A \| B"` nên không tính đúng doanh số từng mã. Bóc từ `du_lieu_json` ĐÃ LƯU SẴN — không gọi lại API sàn, không sửa/xoá gì trên `don_hang`, chạy lại nhiều lần vẫn ra một kết quả. Một bộ tách duy nhất (`src/don-hang-item.js`) dùng cho cả bóc bù lẫn đồng bộ mới (Rule 5). Có đối chiếu tổng dòng với `tong_tien` từng đơn để tự phát hiện bóc sai cấu trúc thay vì im lặng cho ra bảng xếp hạng sai | Có — `migrations/them-donhang-dong.sql` (1 bảng mới + 1 cột cờ trên `don_hang`, chỉ thêm, không phá dữ liệu). **Phải chạy `node scripts/chay-migration.mjs them-donhang-dong.sql --remote` ngay sau khi deploy**, rồi bấm nút bóc bù trong ERP | Không (bảng mới, không đổi bảng/API cũ) | **PILOT** — bóc bù chưa chạy trên dữ liệu thật |
| 2 lỗi tự bắt được khi chạy thử, chưa từng lên production | Kinh doanh | (1) So sánh "kỳ trước" ban đầu dùng "cửa sổ liền trước cùng độ dài" — lúc 9h sáng thì kỳ trước của hôm nay hoá ra là 9 tiếng CUỐI ngày hôm qua, khung giờ mua hàng khác hẳn; đổi sang dịch lùi 1 bước tự nhiên của kỳ (hôm qua đúng giờ này / 7 ngày liền trước / cùng kỳ tháng trước), ghi lý do vào METRIC-DEFINITIONS.md để không ai sửa ngược. (2) Xếp hạng SKU tính cả đơn đã hủy trong khi doanh thu tạm tính thì trừ — cùng 1 màn hình 2 cách tính, vi phạm D4; test cho thấy 1 mã tụt từ hạng 2 xuống hạng 3 sau khi loại đơn hủy, đúng loại sai lệch dẫn tới quyết định đẩy/cắt hàng sai | Không | Không (sửa trước khi lên production) | OFFICIAL |


---

## 2026-08-23 (tiếp)

| Feature | Domain | Decision | Migration | Breaking impact | Status |
|---|---|---|---|---|---|
| Fix Core: dropdown (`ganCombo`) bị modal cắt cụt, không nổi lên trên được | Core UI | Sếp Ngọc báo lỗi thật kèm ảnh chụp (dropdown Danh mục ở Sửa tài sản chỉ hiện 3 dòng rồi cắt cụt). Root cause: `.modal` tự cuộn (`overflow-y:auto`), `.combo1-panel` định vị `position:absolute` bên trong bị modal cắt dù z-index cao — quy luật CSS: overflow trên tổ tiên luôn cắt hậu duệ absolute. Đây là bug ở COMPONENT DÙNG CHUNG (`ganCombo()`, ~20+ chỗ trong ERP), không phải riêng Tài sản — sửa 1 lần, áp dụng tự động cho mọi combobox. Fix: khi mở panel, tính `getBoundingClientRect()` của nút hiển thị rồi chuyển panel sang `position:fixed` với toạ độ tuyệt đối (không bị overflow tổ tiên cắt); đóng khi cuộn trang (`scroll`/`resize` listener trên `window`, capture:true để bắt cả scroll không bubble của phần tử con). Phát hiện thêm 1 bug liên quan khi sửa: listener "click ra ngoài để đóng" dùng chung cho toàn trang trước đó tự ý tắt hiển thị mà KHÔNG gọi đúng hàm đóng riêng của từng combobox — nghĩa là listener scroll/resize vừa thêm sẽ không bao giờ được gỡ khi đóng bằng click-ra-ngoài (chỉ gỡ đúng khi chọn 1 lựa chọn hoặc bấm Escape), dồn mãi trên `window`. Sửa bằng cách gắn `_ganComboDong` lên chính node `.combo1` để listener chung gọi đúng hàm dọn dẹp. Test trực tiếp: mở/đóng bằng click-ra-ngoài 3 lần liên tiếp, đếm add/removeEventListener khớp chính xác 6/6 — không rò rỉ; test cả combobox trong modal (Sửa tài sản) lẫn ngoài trang thường (Thêm tài sản) đều đúng | Không | Không (chỉ đổi cách định vị CSS lúc mở, không đổi API/hành vi chọn) | OFFICIAL |

| Feature | Domain | Decision | Migration | Breaking impact | Status |
|---|---|---|---|---|---|
| Chuẩn hoá "Contextual Creation Rule" — đổi tên ALLOWED/CONTROLLED/FORBIDDEN thành CREATE_DIRECT/CREATE_CONTROLLED/REQUEST_ONLY/NO_CREATE_NEEDED, audit đầy đủ 4 mode | Governance (Core UI) | Chỉ chuẩn hoá tài liệu (`docs/audit/AUDIT-QUICK-CREATE-POLICY.md`, `ERP-CONSTITUTION.md`) theo template audit mới (17 mục: màn hình/field/entity/master-hay-enum/searchable/tần suất/risk/mode) — KHÔNG code thêm. Xác nhận rõ: enum/system dropdown (Trạng thái, Tình trạng, Loại LĐ, Giới tính, Vai trò...) hoàn toàn KHÔNG bị gắn Quick Create (đúng yêu cầu "enum không phải Master Data"). Ghi lại thiết kế Phase 3 (REQUEST_ONLY, bảng `yeu_cau_tao_moi`) để dành khi có nhu cầu thật — chưa xây vì Nhân sự/SKU đã có form tạo đầy đủ riêng, dropdown không phải ngõ cụt thật sự. Phase 2 (Phòng ban/Đơn vị tính/Nhà cung cấp — CREATE_CONTROLLED) vẫn chưa làm, đúng chỉ đạo không refactor hết trong 1 lần | Không | Không | OFFICIAL |

| Feature | Domain | Decision | Migration | Breaking impact | Status |
|---|---|---|---|---|---|
| ERP-wide Quick Create Policy — audit toàn bộ dropdown Master Data + triển khai cho 3 entity rủi ro thấp | Core UI (`ganCombo`) + Governance | Audit trước khi code (`docs/audit/AUDIT-QUICK-CREATE-POLICY.md`): liệt kê toàn bộ ~18 dropdown/searchable-select thật đang có, phân loại ALLOWED/CONTROLLED/FORBIDDEN theo rủi ro (tiền/tồn/quyền, Source of Truth ngoài, cần business owner xác nhận, duplicate nguy hiểm, tần suất). Phát hiện quan trọng: Nhân sự/Vai trò/Sản phẩm-SKU đã ĐÚNG là không có Quick Create ở bất kỳ đâu (FORBIDDEN, đã tự nhiên an toàn); nhưng phát hiện `kvNhapNCC` và `tsThemNCC`/`tsSuaNCC` (Nhà cung cấp) đang là Ô NHẬP TỰ DO chứ không phải reference — vi phạm "No Free Text Fallback", để dành sửa riêng (CONTROLLED, không phải rủi ro thấp). Mở rộng `ganCombo()` (dùng ở toàn bộ ~20 chỗ trong ERP) thêm tham số `taoMoi` tuỳ chọn — không tìm thấy + đang gõ có chữ → hiện "+ Tạo '...'", bấm vào gọi thẳng API `themXyz` đã có sẵn (dùng lại Search Before Create/`canh_bao` gần giống đã có ở `dulieunen.js`, không viết logic duplicate mới), tạo xong tự chọn record mới, không đóng modal cha, không reload, không mất field khác đang nhập. Bật cho 3 entity ALLOWED có sẵn hạ tầng: **Danh mục tài sản, Vị trí tài sản, Chức danh** — cùng người bấm vào các form này (Data Owner tương ứng) vốn đã đúng quyền tạo Master Data đó, field bắt buộc duy nhất là tên đang gõ nên không cần mini-form riêng. Test trực tiếp: tạo mới tự chọn đúng, gõ gần giống ("Máy   tính" vs "Máy tính") → backend bắt đúng → confirm() → từ chối → không tạo trùng, xoá dữ liệu test sau khi verify | Không | Không (chỉ thêm tham số tuỳ chọn cho hàm dùng chung, các lời gọi cũ không truyền `taoMoi` không đổi hành vi) | OFFICIAL |

---

## 2026-08-23

| Feature | Domain | Decision | Migration | Breaking impact | Status |
|---|---|---|---|---|---|
| Module Tài sản Phase 1 — QR/mã vạch, in tem, Danh mục/Vị trí Master Data, Sửa, Chi tiết, Điều kiện tách khỏi Trạng thái | Domain (Tài sản, HCNS/Admin) + Core UI (QR, print) | Audit trước khi code (`docs/audit/AUDIT-TAISAN-MODULE.md`): module Tài sản đã có sẵn từ trước (tai_san/tai_san_lich_su, kiến trúc Master/History đã đúng), 0 dòng dữ liệu thật trên production — Extend chứ không Create. Thêm 2 bảng Master Data mới (`tai_san_danh_muc`/`tai_san_vi_tri`, cùng khuôn Data Lock với Phòng ban/Chức danh — reuse `veDanhMuc()` đã rút thành hàm dùng chung). Thêm trạng thái `mat` (LOST) tách khỏi `bao_hong` (Sếp chốt: hỏng còn sửa được, mất thì không, khác nghiệp vụ) và field `tinh_trang` (Condition) tách khỏi `trang_thai` (Status). QR code sinh bằng thư viện vendor `qrcode-generator` (MIT, xem `docs/THIRD-PARTY-LIBRARIES.md`) — chỉ encode Mã tài sản (`ma_ts`), không encode giá/người giữ. Quét bằng `BarcodeDetector` API gốc trình duyệt (không vendor decoder) — nơi chưa hỗ trợ thì nhập tay mã, không chặn luồng. In tem bằng HTML/CSS `@media print`, không cần PDF engine. Đã test trực tiếp trên trình duyệt: tạo tài sản → sinh mã tự động (TS0001) → QR hiện đúng → tra cứu theo mã (kể cả chữ thường) mở đúng tài sản → Sửa/Báo mất/Bảo trì xong/lịch sử cập nhật đúng, không cần F5 → xoá dữ liệu test sau khi verify. **Chưa verify được**: quét QR thật bằng camera điện thoại thật (môi trường dev không có camera) — cần Sếp tự thử in + quét trước khi coi là OFFICIAL, đúng tinh thần PILOT trước OFFICIAL đã có trong Go-Live Level. Phát hiện thêm 1 bug Core: `form.reset()` không xoá được input ẩn của `ganCombo()` do quirk trình duyệt (set `.value` qua JS làm `defaultValue` đổi theo) — đã fix cho 2 field mới, CHƯA audit các `ganCombo()` khác trong ERP (rủi ro thấp, để dành riêng) | Có — `migrations/them-taisan-mo-rong.sql` (2 bảng mới + 13 cột mới trên `tai_san`, không phá dữ liệu vì 0 dòng thật) | Không (mở rộng schema, giữ nguyên field/API cũ) | PILOT |
| Tem tài sản khổ vật lý chính xác 60×40mm ngang (template `ASSET_LABEL_60X40`) + Lưu &amp; In tem + In hàng loạt | Domain (Tài sản) + Core UI (print) | Sếp chốt khổ cuối cùng 60mm rộng × 40mm cao (không phải 40×60 như bàn đầu) — sửa `@page { size: 60mm 40mm; margin: 0 }` + container `.ts-tem` đúng 60mm×40mm (đã verify bằng `getComputedStyle` khớp chính xác phép quy đổi mm→px, không chỉ nhìn preview). Layout ngang: trái là tên/mã/công ty, phải là QR to. Không in giá/người giữ/phòng ban/vị trí lên tem (đổi theo thời gian, quét mới ra dữ liệu hiện tại — đúng yêu cầu). Tên tài sản dài tự xuống dòng tối đa 2 dòng rồi cắt (`-webkit-line-clamp`), không co QR lại. Thêm nút **"Lưu &amp; In tem"** cạnh "+ Thêm tài sản" (phân biệt qua `SubmitEvent.submitter`, không cần biến cờ riêng) — tạo xong in luôn, không bắt quay lại danh sách tìm. Thêm **chọn nhiều dòng → In tem hàng loạt** trong Danh sách tài sản (mỗi tài sản 1 trang in, `page-break-after`, không ghép chung 1 tem). Reprint (In tem lại từ Chi tiết) dùng nguyên `ma_ts`/`ten` đã có, không sinh mã mới, không ghi thêm lịch sử (Sếp: "không cần spam Audit nếu việc này không có giá trị"). Test trực tiếp: in 1 tem, in 2 tem hàng loạt, Lưu&amp;In tem tạo tài sản mới — cả 3 xác nhận đúng nội dung + đúng kích thước CSS, đã xoá dữ liệu test | Không (chỉ CSS + JS, không đổi schema/API) | Không | PILOT — vẫn chờ Sếp in thử bằng máy in tem thật (chưa test được máy in vật lý trong môi trường dev) |

| Feature | Domain | Decision | Migration | Breaking impact | Status |
|---|---|---|---|---|---|
| Governance layer (Constitution, Data Dictionary, Source of Truth, Module Map, templates, CLAUDE.md) | Core (process, không phải code) | Chuyển từ 1 người code sang mô hình ERP Owner / Domain Owner / Developer Agent / Key User — Central Architecture + Distributed Business Ownership | Không | Không — chỉ thêm tài liệu, không đổi code/schema | OFFICIAL |
| Rà soát hoàn chỉnh governance layer so với yêu cầu gốc (38 phần) | Core (process) | Bổ sung ARCHITECTURE.md, cột Owner/Dependencies/Public API còn thiếu trong MODULE-MAP.md, Data Input Rule, Module Retirement lifecycle, UI Consistency catalog, Pre-Code Checklist đầy đủ trong CLAUDE.md; sửa 1 tên hàm đã lỗi thời (`ganComboNhanSu` → `ganCombo`) sót lại trong UX_ENGINEERING_STANDARD.md | Không | Không | OFFICIAL |
| Đổi vai trò hệ thống: Giám đốc/Phó Giám đốc/Admin hệ thống → Admin/Admin backup/Người dùng | Core (Permission) | Sếp chốt — gộp lãnh đạo thành 1 vai trò Admin, thêm Admin backup cho HR/trưởng phòng tạo tài khoản hộ, có chặn leo thang quyền | Không (đổi dữ liệu `tai_khoan.vai_tro`, không đổi schema) | Có — role key cũ (`giam_doc`, `pho_giam_doc`, `admin_he_thong`) không còn hợp lệ, đã cập nhật 2 tài khoản thật trước khi deploy | OFFICIAL |
| Search + Filter cho Nhân sự/Tài sản/Tài khoản, thay `prompt()` bằng modal | Domain (HR, Tài sản, Quản trị) | Chuẩn hoá theo `UX_ENGINEERING_STANDARD.md`, client-side vì dữ liệu nhỏ | Không | Không | OFFICIAL |
| Searchable Combobox (`ganCombo()`) thay native `<select>` dài cho mọi chỗ chọn nhân sự/sản phẩm/vai trò/chức danh/phòng ban | Core UI component | 1 control duy nhất, search tích hợp trong dropdown, ngưỡng ≥7 lựa chọn hoặc danh mục có thể tăng | Không | Không (giữ nguyên `.value` semantics qua `<input type="hidden">` + `change` event) | OFFICIAL |
| Fix UX gây hiểu lầm: modal "+ Giao Mục Tiêu" tạo Mục tiêu | Domain (MBO/Task) | Sếp Ngọc phản ánh tự giao "mục tiêu" cho mình nhưng không thấy ở Trạm Mục Tiêu cấp cá nhân — root cause: field "Thuộc mục tiêu" (nơi tạo/gắn `muc_tieu`) bị ẩn mặc định trong khối "+ Thêm tuỳ chọn" đã gấp lại, nên bấm "+ Giao Mục Tiêu" và điền các ô hiện có chỉ tạo `cong_viec` (Task), không tạo `muc_tieu`. Fix: đưa "Thuộc mục tiêu" ra luôn hiện sẵn trong form chính, chỉ giữ Người phối hợp/Ghi chú trong "+ Thêm tuỳ chọn". Cũng thêm chip "🎯 Thuộc mục tiêu: ..." trên các dòng Việc đã gắn mục tiêu, và gộp mục tiêu Đã xong vào khối gấp lại riêng trên Trạm Mục Tiêu (không đụng `cvLichSu` — Việc/Mục tiêu vẫn 2 khái niệm tách biệt hoàn toàn ở tầng dữ liệu, không chồng chéo) | Không | Không (chỉ đổi vị trí field trong DOM + text, giữ nguyên id/logic JS) | OFFICIAL |
| Fix UI State Consistency: "Lịch sử làm việc" và "Tổng quan công ty" (Admin) không cập nhật ngay sau khi tạo/hoàn thành/huỷ Việc, phải F5 mới đúng | Domain (Task) + Core UI pattern | Root cause: `khoiDongLichSuViec()`/`taiLaiTongQuanCongTy()` chỉ fetch dữ liệu 1 lần lúc tải trang, `moTab()` chuyển tab không tự fetch lại — mutation ở module Việc không có đường nào báo cho 2 màn này biết cần tải lại. Tái hiện thật bằng test data trên local D1, xác nhận trước fix: đã tạo/hoàn thành việc nhưng cả 2 màn vẫn hiện `0`/dữ liệu cũ tới khi F5. Fix: gộp `lamMoiCacManLienQuanCv()` trong `khoiDongCongViec` (tải lại Việc cần làm/giao/phối hợp + Tổng quan công ty + gọi `window.LAM_MOI_LICHSU_VIEC` nếu có) dùng ở cả 3 nơi tạo/nộp/đổi trạng thái Việc; export `window.LAM_MOI_LICHSU_VIEC` từ `khoiDongLichSuViec`. Verify lại bằng test data: cả 2 màn cập nhật đúng ngay không cần F5, đã xoá test data sau khi verify. Ban hành rule chung **UI State Consistency** trong `ERP-CONSTITUTION.md` Rule 7 + `UX_ENGINEERING_STANDARD.md` (naming convention `window.LAM_MOI_*`) + thêm mục vào `DEFINITION-OF-DONE.md`. Audit nhanh các module khác: phát hiện `khoiDongLichSuHoan` (Lịch sử hoàn, dưới Đơn hoàn) có cùng pattern rủi ro (fetch 1 lần, không có `LAM_MOI_*`) nhưng CHƯA sửa trong đợt này — nguyên nhân: mutation nguồn nằm rải ở 3 module (Đơn hoàn/Kế toán trả soát/Kế toán hàng hỏng), sửa đúng cần đụng cả 3 chỗ nên không còn là fix "rủi ro thấp" — để dành làm riêng khi có nhu cầu thật | Không | Không (chỉ đổi thời điểm gọi API refetch, không đổi API/schema) | OFFICIAL |
| Chuẩn hoá Home/Dashboard toàn ERP (Phase 1: Home role-based + Dashboard Rules + Drill-down) | Core (Home) + Governance | Audit đầy đủ trước khi code (`docs/audit/AUDIT-HOME-DASHBOARD.md`) theo yêu cầu "Home theo người dùng + Tổng quan theo Domain + Dashboard chỉ khi có quyết định". Phát hiện thật từ dữ liệu production: Home cũ chỉ có 2 mức Admin/không-Admin, trong khi trưởng phòng thật (Phạm Khương Duy — P. Kho Vận-Sản Xuất) có `vai_tro` hệ thống chỉ là "nguoi_dung" — nếu tiering theo vai_tro sẽ bỏ sót đúng người cần view Manager. Thêm tầng thứ 3: Employee (Action First, giữ nguyên) / **Manager MỚI** (Exception First — team quá hạn/chờ duyệt của đúng phòng ban họ quản lý, xác định qua `TOI.phong_ban_quan_ly` = cơ chế `phong_ban.truong_phong_id` đã có, KHÔNG theo vai_tro) / CEO-Admin (Decision First, giữ nguyên). API mới `GET /api/cong-viec/tong-quan-phongban` (mirror `cvTongQuanCongTy`, tự đọc phòng ban từ session — không nhận từ client). Thêm Drill Down to Action: `veThe()`/`veDanhSach()` hỗ trợ `onClick` (mọi nơi dùng chung, backward-compatible); `window.MO_DEN_VIEC_CUA_TOI` (việc của chính người xem — scroll+highlight đúng dòng) và `window.MO_DEN_LICHSU_TIM` (việc của người khác — nhảy sang Lịch sử làm việc, tự tìm sẵn). Ban hành **Dashboard Rules D1-D6** vào `ERP-CONSTITUTION.md` + khởi tạo `docs/METRIC-DEFINITIONS.md`. Verify bằng test data thật trên local D1 (tạo phòng ban + trưởng phòng + việc quá hạn/chờ duyệt test, đăng nhập thật bằng tài khoản test), xác nhận Manager thấy đúng team, KHÔNG thấy khối Admin, click "Cần chú ý" nhảy đúng Lịch sử làm việc — xoá test data sau khi verify. Audit xác nhận domain khác (Kinh doanh/Kế toán/Đơn hoàn/Nhân sự/Xếp ca/Tài sản) đã đúng dạng Work Queue, KHÔNG cần Tổng quan — không đụng. Phase 2-4 (Kho vận/Marketplace/CEO Tổng quan riêng) CHƯA làm — chờ pilot xác nhận Phase 1 | Không | Không (API mới, không đổi API/schema cũ; `veThe`/`veDanhSach` thêm tham số tuỳ chọn, tương thích ngược) | PILOT |

## Trước 23/08/2026

Xem `docs/audit/AUDIT-KIEN-TRUC.md` §K (roadmap Phase 0-5) và `git log`
cho lịch sử chi tiết ~90 commit trước ngày này — không chép lại ở đây,
tránh trùng lặp.

**Mốc lớn đã có trước đó** (tóm tắt, không chi tiết từng commit):
- Migration safety: `schema_migrations` + `chay-migration.mjs` +
  `kiem-tra-migration.mjs` (Phase 1, đã hoàn thành, xem AUDIT-KIEN-TRUC.md).
- Mã nhân sự (`ma_nv`) theo Loại lao động, sinh tập trung qua
  `src/dinh-danh.js`.
- Data Ownership / Data Lock pattern áp dụng cho 7 bảng danh mục nền.
- Module Tài sản (Asset Management) — mới hoàn toàn.
- Auto Allocation engine cho Đăng ký ca (Xếp ca).
- Bulk import 21 nhân sự thật từ bảng tính.
