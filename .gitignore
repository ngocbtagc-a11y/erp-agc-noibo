# Phụ thuộc
node_modules/

# Cấu hình bí mật — TUYỆT ĐỐI không đưa lên GitHub
.env
.env.*
*.key
*.pem
config.local.*
secrets.*
.dev.vars

# seed.sql chứa hash mật khẩu của nhân sự — không đẩy lên
# (chạy `npm run tao-tai-khoan` để sinh lại khi cần)
seed.sql

# Hệ điều hành
Thumbs.db
desktop.ini
.DS_Store

# Trình soạn thảo
.vscode/
.idea/

# Nhật ký
*.log
npm-debug.log*

# Cloudflare
.wrangler/

# Bí mật + trạng thái cục bộ của Cloudflare Wrangler
.dev.vars
.dev.vars.*
.wrangler/

# Bản clone lồng nhau tạo nhầm — không đưa vào git
erp-agc-noibo/
.thu-sao-luu/
.thu-khoi-phuc/
.thu-gop-thang/

# Thư mục tạm của bàn đo: bản src bị làm hỏng cố ý (ca đối chứng BH-16) và
# bản src cũ dựng ra để lấy số ĐO TRƯỚC. Bàn đo tự tạo, tự xoá.
.dc-*/
.src-cu/
